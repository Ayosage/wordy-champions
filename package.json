{
  "name": "wordy-champions",
  "private": true,
  "packageManager": "pnpm@10.12.1",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test --concurrency=1",
    "lint": "turbo run lint",
    "dev": "turbo run dev"
  },
  "devDependencies": { "prettier": "^3.3.0", "turbo": "^2.3.0", "typescript": "^5.6.0" },
  "pnpm": { "onlyBuiltDependencies": ["esbuild", "workerd", "@ayosage/match-core"] }
}
